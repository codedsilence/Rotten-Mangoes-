<h2> All Users: </h2>

<hr>
<% @users.each do |user| %>
<div>
  <p>ID: <%= user.id%> <%= user.full_name%>: <%= user.email %></p> 
  <p><small>created: <%= user.created_at %></small></p>

  <p> <%= link_to "Delete", admin_user_path(user), method: :delete, confirm: "You sure?" %> | <%= link_to "Edit", edit_admin_user_path(user.id), confirm: "You sure?" %> <p>
</div>
<hr>
<% end %>